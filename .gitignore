# Secrets — never commit (GitHub push protection blocks API keys in history)
.env
.env.*
!.env.example

# Large binaries / datasets — never commit (GitHub 100MB limit; huge clone/push cost)
Qwen3-0.6B-Base/model.safetensors
nv-community_Nemotron-CC-Math-v1_4plus_first100000.jsonl

# LoRA weight blobs (monorepo safety net; prefer stage-local .gitignore for wandb/data)
**/adapter_model.safetensors
**/adapter_model.bin
